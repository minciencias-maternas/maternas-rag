# ============================================================
# Chatbot RAG Maternas — Requirements
# Python 3.12.7 | Generado el 31 de mayo de 2026
# Instalado y verificado con: pip check → No broken requirements
# ============================================================

# --- API & Web ---
fastapi==0.115.6
uvicorn==0.32.1
starlette==0.41.3
pydantic==2.10.3
pydantic-settings==2.14.1
python-dotenv==1.0.1
python-multipart==0.0.20     # requerido por FastAPI para UploadFile/File(...)
httpx==0.28.1                # cliente HTTP de la UI y de fastapi.testclient

# --- LangChain ---
langchain==0.3.13
langchain-community==0.3.13
langchain-core==0.3.63
langchain-text-splitters==0.3.8
langchain-groq==0.2.3

# --- LLM Clients ---
groq==0.13.1

# --- Embeddings & Vector Store ---
# sentence-transformers==3.3.1 produce un cuelgue silencioso al importar
# junto con torch (ver docs/DOCUMENTACION.md, seccion 14) — pineado a 2.7.0.
sentence-transformers==2.7.0
faiss-cpu==1.9.0
transformers==4.57.6
torch==2.12.0
tokenizers==0.22.2
rank-bm25==0.2.2             # capa lexica del retrieval hibrido (Config F)

# --- Bot ---
python-telegram-bot==21.10

# --- Privacidad (cifrado de active_users.json) ---
cryptography==48.0.0

# --- Scheduler ---
apscheduler==3.10.4

# --- Evaluación ---
ragas==0.2.12

# --- UI ---
streamlit==1.41.1

# --- Data & Utils ---
numpy==2.4.6
pandas==2.3.3
tqdm==4.67.3
requests==2.34.2

# --- Testing ---
pytest==9.1.1
